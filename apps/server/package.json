{
  "name": "server",
  "private": true,
  "scripts": {
    "install": "python3 -m venv .venv && pip install -r requirements.txt && source .venv/bin/activate",
    "windows:install": "python -m venv .venv && pip install -r requirements.txt && source .venv/Scripts/activate",
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
